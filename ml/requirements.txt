# Hermes Crypto Radar — ML Pipeline Dependencies
#
# Install with:
#   pip install -r ml/requirements.txt  
# TODO: Use a virtual environment to avoid polluting the global Python environment.
#
# For GPU training (requires CUDA toolkit):
#   pip install lightgbm[cuda]>=4.6.0  
#   # or use the conda-forge channel:
#   conda install -c conda-forge lightgbm cudatoolkit 
# TODO: Consider using a virtual environment (venv) or conda environment to avoid polluting the global Python environment.
# Core ML
lightgbm>=4.6.0,<5.0                # Gradient boosting — 4.x API (early_stopping via callbacks)

# Data processing
pandas>=3.0.3,<4.0                  # DataFrame I/O — 3.x series
numpy>=2.1.5,<3.0                  # Array operations

# ML utilities
scikit-learn>=1.9.0,<2.0              # Metrics, preprocessing, train_test_split
joblib>=1.5.3,<2.0                  # Model serialization (.joblib)

# Performance (optional — speeds up CSV parsing by 10-30x)
pyarrow>=25.0.0,<26.0.0  # Arrow-backed pandas CSV reader
