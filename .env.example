# ═══════════════════════════════════════════════════════════════════════
# Hermes Crypto Radar — Environment Configuration
# ═══════════════════════════════════════════════════════════════════════
# All env vars are prefixed with RADAR__ and are OPTIONAL.
# Uncomment and set to override defaults.
#
# Run: RADAR__LOG_LEVEL=debug crypto-radar scan
# ═══════════════════════════════════════════════════════════════════════

# ── Core Configuration ───────────────────────────────────────────────

# Data directory for logs and cached state
# Default: ~/.hermes/data/crypto-radar
# RADAR__DATA_DIR=~/.hermes/data/crypto-radar

# Log level: trace, debug, info, warn, error, fatal
# Default: info
# RADAR__LOG_LEVEL=info

# Token whitelist (JSON array or comma-separated) — if set, only these tokens are scanned
# Default: all known tokens
# RADAR__TOKENS=["bitcoin","ethereum","solana"]

# ── API Configuration ────────────────────────────────────────────────

# Binance data API base URL
# Default: https://data-api.binance.vision
# RADAR__BINANCE_BASE_URL=https://data-api.binance.vision

# HTTP request timeout in milliseconds
# Default: 10000
# RADAR__FETCH_TIMEOUT_MS=10000

# Cache TTL in milliseconds
# Default: 300000 (5 minutes)
# RADAR__CACHE_TTL_MS=300000

# ── Signal Engine Configuration ──────────────────────────────────────

# Strategy weights (JSON) for signal scoring
# Default: {"momentum":0.4,"mean-reversion":0.2,"trend-following":0.4}
# RADAR__STRATEGY_WEIGHTS={"momentum":0.4,"mean-reversion":0.2,"trend-following":0.4}

# Timeframe weights (JSON) for multi-timeframe aggregation
# Default: {"15m":0.1,"1h":0.25,"4h":0.3,"1d":0.35}
# RADAR__TIMEFRAME_WEIGHTS={"15m":0.1,"1h":0.25,"4h":0.3,"1d":0.35}

# DeFiLlama on-chain metrics enabled during scan
# Default: false
# RADAR__DEFI_LLAMA_ENABLED=true

# ── Daemon Configuration ─────────────────────────────────────────────

# HTTP port for the warm daemon (persistent background process)
# Default: 9877
# RADAR__DAEMON_PORT=9877

# Daemon cache refresh interval in seconds
# Default: 300
# RADAR__DAEMON_REFRESH_SEC=300

# ── WebSocket Configuration ──────────────────────────────────────────

# WebSocket stream port for real-time price updates
# Default: 9878
# RADAR__WS_PORT=9878

# ── Alert / Webhook Configuration ────────────────────────────────────

# Webhook URL for price alerts (Discord webhook or Telegram bot API)
# Example (Discord): https://discord.com/api/webhooks/...
# Example (Telegram): https://api.telegram.org/bot<TOKEN>/sendMessage
# RADAR__WEBHOOK_URL=

# Webhook type: discord or telegram
# Default: discord
# RADAR__WEBHOOK_TYPE=discord

# ── Data Retention Configuration ─────────────────────────────────────

# Automatically prune CSV logs older than N days
# Default: 0 (disabled — logs are kept indefinitely)
# RADAR__LOG_RETENTION_DAYS=30

# ── Fastify API Server ───────────────────────────────────────────────

# JWT secret for auth token signing (REQUIRED for auth endpoints)
# Generate with: openssl rand -hex 32
# Default: 'dev-secret-change-in-production'
# RADAR__JWT_SECRET=your-256-bit-hex-secret-here

# JWT audience claim (optional, for production validation)
# RADAR__JWT_AUDIENCE=https://crypto-daemon.railway.app

# JWT issuer claim (optional, for production validation)
# RADAR__JWT_ISSUER=hermes-crypto-radar

# ── Log Format ───────────────────────────────────────────────────────

# Log format: 'text' for human-readable (CLI default) or 'json' for structured
# Default: text
# RADAR__LOG_FORMAT=text

# ── ML Pipeline Configuration ────────────────────────────────────────

# Enable the ML pipeline
# Default: false (disabled)
# RADAR__ML_ENABLED=true

# Lookback window in days for training data
# Default: 90
# RADAR__ML_LOOKBACK_DAYS=90

# Auto-retrain interval in hours
# Default: 24
# RADAR__ML_RETRAIN_HOURS=24

# Minimum prediction confidence (0–1) to surface a signal
# Default: 0.6
# RADAR__ML_MIN_CONFIDENCE=0.6

# Enable hyperparameter optimization (true/false)
# Default: not set (opt-in)
# RADAR__ML_OPTIMIZE=true

# Cross-validation folds for training
# Default: not set (opt-in)
# RADAR__ML_CV_FOLDS=5

# Enable class balancing (true/false)
# Default: not set (opt-in)
# RADAR__ML_BALANCE=true

# Enable SHAP feature importance (true/false)
# Default: not set (opt-in)
# RADAR__ML_SHAP=true
