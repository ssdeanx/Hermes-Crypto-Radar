# Prism Constraint History — Hermes Crypto Radar

Auto-generated ledger of `/prism-scan` and `/prism-reflect` analyses on this project.
Used by `/prism-scan` (Step 0) to avoid repeating blind spots. Append, never rewrite.

---

## Entry 1 — 2026-07-08 — prism-scan (signal-engine audit)

**Lens focus:** Scoring-math correctness + silent ranking corruption in `signals.ts` / `radar.ts`.
**Maximized:** numeric/silent corruption in the additive composite engine.
**Sacrificed (did NOT examine):** network/resilience & rate-limit behavior (`binance.ts`,
`core/circuit-breaker.ts`); persistence/SQLite schema integrity (`store/`).

### Findings raised (id → severity → fixable?)
- #1 missing news scored 0 (not neutral) → 20pt silent under-rank of low-coverage tokens — High — FIXED
- #2 divergence mixes 24h ticker rangePos with 1h RSI → false HIGH alerts — High — FIXED
- #3 no NaN guards in `enrichTicker`/`parseFloat` → single bad feed field poisons composite + sort — Med-High — FIXED (safeParseFloat + NaN-safe clamps/sorts)
- #4 additive boosts clamped at 100 while penalties always bite → on-chain/agreement boosts inert at top — Medium — FIXED (post-clamp multiplicative boost layer)
- #5 ADX 0.6× multiplier also scaled on-chain boost → double penalty in chop — Medium — FIXED (ADX applied to score, not boost)
- #6 two parallel signal engines (computeSignals vs StrategyEngine); table ≠ "Top Signals" authority — Med-High — PARTIAL: left engines separate (consolidation is architecturally risky), documented divergence; not regressed
- #7 coarse/binary technical buckets mismatch raw-direction logic — Low-Med (tuning) — NOT FIXED (acceptance risk, out of scope)

### Conservation law discovered
> Any additive scoring system that absorbs both normalized 0–100 sub-scores and unbounded
> ±percentage-point adjustments will, under a final clamp, convert legitimate rewards into
> no-ops while letting penalties through — unless boosts are applied in a post-clamp
> multiplicative layer. Holds across all implementations of additive scoring.

### Outstanding gaps for next scan
- Temporal/maintenance drift: how do these scoring weights calcify over many releases?
- Network resilience path: does a partial Binance outage silently produce `NaN`/empty
  technicals that the new guards correctly neutralize? (verify in `binance.ts` integration.)
- Persistence: are `composite_score`/`NaN` values ever written to SQLite `store/`?
