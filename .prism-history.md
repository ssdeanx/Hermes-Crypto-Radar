# Prism Constraint Ledger

## Entry 1 — 2026-07-11 — prism-scan (whole-project, JSONL)

**Maximized:** The additive scoring composite pipeline (`src/signals.ts computeSignals`) and its cross-layer boost/penalty application; traced two adversarial tokens end-to-end; audited every `.sort()` comparator for NaN-safety.
**Sacrificed:** (1) live upstream feed/parsing resilience beyond `safeParseFloat`; (2) the backtest/paper-trade decision loop that consumes `compositeConfidence`.
**Findings:** F1 → high → fixable | F2 → high → structural | F3 → medium → fixable | F4 → medium → fixable | F5 → medium → fixable | F6 → low → fixable | F7 → low → structural | F8 → low → structural | F9 → low → structural
**Conservation law:** Any additive scoring engine absorbing both 0–100 sub-scores and ±pp adjustments with a final clamp + a second multiplicative boost layer will over-reward if a bonus is placed in BOTH layers. The attractor is "every reward lives in exactly one layer" — violated here for the agreement bonus (F1). The two-authority divergence (F2: compositeScore 0–100 vs compositeConfidence 0–1) is a structural conservation: a codebase that grew two scoring engines cannot be cheaply unified without rewiring consumers.
**Outstanding gaps:** Next scan should weight toward (a) temporal/maintenance drift of the dual-engine divergence, (b) the backtest/paper-trade loop's handling of NaN/scale, (c) whether `newsScore=0` display reconciliation (F3) regresses any pinned exporter test.

---

## Entry 2 — 2026-07-11 — prism-full (dashboard & enterprise readiness, JSONL)

**Maximized:** Audited every REST endpoint (16 routes in `src/api/rest.ts`) and WS channel (`src/api/ws.ts`) for frontend-readiness; mapped 8 dashboard views to API surface; analyzed deployment/auth/monitoring/security for multi-user production. Built go/no-go matrix, component-to-endpoint traceability, and infrastructure gap analysis.
**Sacrificed:** (1) live frontend framework comparison (React/Next.js vs Svelte vs Vue); (2) actual API integration tests — this is a spec-level audit, not a test run.
**Findings added:** F10–F23 (14 new findings covering API gaps, infrastructure, and architecture).
**Conservation law:** The REST API covers ~80% of what a dashboard v1 needs today — 15 of 16 endpoints are consumable with minor adapter work. The remaining 20% is infrastructure (auth, OpenAPI, Docker, deployment model) rather than data gaps. The deepest architectural finding is D23: serving the dashboard from the daemon itself (single-process Grafana pattern) eliminates 6 infrastructure gaps in one decision.
|**Retracted (from analysis):** signals/:symbol O(n) claim — 49-token scale makes it negligible. Portfolio 30%-ready claim — paper trading portfolio is correctly implemented; real portfolio is an unbuilt feature. CORS missing claim — daemon sets headers before delegating to REST handler.
|**Outstanding gaps:** Next scan should weight toward (a) whether serving frontend from the daemon (F23) creates path traversal or caching concerns; (b) whether the /api/v1/ prefix (F21) conflicts with existing Hermes agent tool references; (c) whether hard-coded $100k cash (F9) breaks any existing paper-trade test assertion.
|
|**Remediated (2026-07-12):** F10 (medium), F11 (low), F14 (low), F15 (low) — REST API additions: chain filter on GET /api/tickers, symbol filter on GET /api/signals, GET /api/tokens endpoint, GET /api/regime/:symbol endpoint.
|
|---|
|
|## Entry 3 — 2026-07-12 — prism-scan (scale-boundary: paper-trade loop vs dual-engine divergence)
|**Maximized:** Traced every confidence/score value across the three boundary crossings between AggregatedSignal (0–1) and TokenSignal (0–100) and the paper-trade decision loop (`getSignalRecommendations` → `agentPlay`). Walked a high-confidence signal (0.85) through all arithmetic; constructed one inverted fix scenario.
|**Sacrificed:** (1) backtest equity-curve NaN handling; (2) paper-trade SharpeRatio using raw dollar P&L instead of percentage returns (position-size dominance).
|**Findings:** F1 → critical → fixable | F2 → high → fixable | F3 → high → fixable | F4 → medium → structural | F5 → low → fixable | F6 → medium → fixable
|**Conservation law:** Any codebase with two scoring engines producing different numeric scales for semantically identical values (TokenSignal.compositeScore 0–100 vs AggregatedSignal.compositeConfidence 0–1) and a downstream consumer reading from both will accumulate one scale-confusion site per boundary crossing. No type-system enforcement can prevent this because both ranges are `number`. The attractor is "normalize at the absolute boundary with assertion guards."
|**Outstanding gaps:** Next scan should weight toward (a) whether backtest equity-curve arithmetic (division by zero, NaN propagation) affects reported Sharpe at low signal counts; (b) whether paper-trade SharpeRatio using $P&L rather than % returns makes it prefer large positions over good timing.
|
|**Remediated (2026-07-12):** F1 (critical), F2 (high), F3 (high), F5 (low), F6 (medium) — scale-boundary bugs fixed in paper-trade.ts (`compositeConfidence/100` removed, `compositeScore` normalized, assertion guards added) and backtest.ts (`minConfidence*100` removed, `Number.isFinite` guard). F4 left as structural (dual-engine divergence documented).
